Flask==2.3.2
Werkzeug==2.3.6
Jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.3
gunicorn==21.2.0   # optional, for deployment

# GITA PYQ Repository (JSON Version)

A simple Flask-based web app to manage and browse **GITA Autonomous College previous year question papers**.  
This version uses a JSON file (`papers.json`) for storing metadata instead of a database, making it lightweight and beginner-friendly.

---

## ✨ Features
- Upload question papers (PDFs, images).
- Browse by **Year → Semester → Subject**.
- Separate **Mid-Sem (25)** and **End-Sem (100)** categories.
- Prevents accidental overwriting by keeping unique filenames.
- Clean and simple UI built with **Flask + Jinja2 + HTML/CSS**.
- Static assets (campus image, logo) included for branding.

---

## 🛠 Tech Stack
- **Backend:** Flask (Python)
- **Frontend:** HTML, CSS, Jinja2
- **Storage:** JSON (`papers.json`)

---

## 📂 Project Structure
gitapapers/
app.py
templates/
base.html
index.html
year.html
semester.html
subject.html
static/
css/
style.css
images/
gita-campus.jpg
logo.png
uploads/ # (ignored in Git)
data/
papers.json # stores uploaded papers metadata